# Basic configuration
spring.application.name=viagens-api
server.port=8080

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/viagens?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=root

# Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Environment configuration
spring.profiles.active=dev

# Files configuration
max.file.size=524288000
min.file.size=2048
max.file.name.length=200
min.file.name.length=5


# Swagger configuration -- TODO: Add Swagger configuration
